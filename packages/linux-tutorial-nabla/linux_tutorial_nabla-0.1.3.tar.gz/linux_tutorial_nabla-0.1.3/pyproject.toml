[project]
name = "linux_tutorial_nabla"
version = "0.1.3"
description = "Interactive Linux tutorial for beginners"
authors = [
    { name = "Martin Sundal Aspås", email = "martin.s.aspas@gmail.com" },
]
dependencies = [
    "pydantic>=2.9.2",
    "colorama>=0.4.6",
]
requires-python = ">=3.10"
readme = "README.md"

[project.license]
text = "MIT"

[project.scripts]
linux_tutorial_nabla = "linux_tutorial_nabla.__main__:main"

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[tool.pdm]
distribution = true
