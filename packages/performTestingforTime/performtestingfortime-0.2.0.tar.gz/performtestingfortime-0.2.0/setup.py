from setuptools import setup, find_packages

setup(
    name = 'performTestingforTime',
    version = '0.2.0',
    description = 'Tests the performance of functions according by time',
    author = 'Tolga',
    packages=find_packages(),
)