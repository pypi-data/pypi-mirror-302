[tool.poetry]
name = "fleetglue-client"
version = "2.0.2"
description = "Python client for FleetGlue API"
authors = ["Pablo Díaz Ogni <pablo@fleetglue.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
requests = "^2.28.1"

[tool.poetry.group.test.dependencies]
click = "^8.1.7"
pytest = "^8.3.3"
pytest-cov = "^5.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
fleetglue = 'fleetglue_client.cli.fleetglue:cli'
