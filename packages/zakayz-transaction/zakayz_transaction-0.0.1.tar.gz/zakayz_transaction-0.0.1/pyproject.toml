[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "zakayz-transaction"
version = "0.0.1"
dependencies = [
  "pandas",
]
requires-python = ">=3.8"

[project.scripts]
transaction-sum = "transaction_count:main"
