class RequestInterceptor:
    def intercept(self, request: 'Request') -> 'Request':
        """Intercept and optionally modify a request before it's sent."""
        # Implementation to intercept and modify requests
        return request
