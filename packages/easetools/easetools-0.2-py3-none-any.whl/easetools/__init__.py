from .module1 import (
    add, sub, mul, div, 
    maximum, minimum, mean, median, mode, variance, 
    gcd, lcm, isprime, primes, factors, factorial, 
    permutation, combination, subarray, 
    transpose, determinant, inverse, dotproduct
)
