[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "toml_hello_module"
version = "0.1.0"
description = "description"

[tool.setuptools]
py-modules = ["hello"]
