
[build-system]
requires = ["setuptools>=61.0", "versioningit"]
build-backend = "setuptools.build_meta"


# ----------------------------------------- setuptools -------------------------------------------
[tool.setuptools]
include-package-data = true
zip-safe = false

[tool.setuptools.packages.find]
where = ["src"]
namespaces = true


# ----------------------------------------- Project Metadata -------------------------------------
#
[project]
version = "0.0.0.dev9"
name = "LicenseMan"
requires-python = ">=3.10"
dependencies = [
    "LoggerMan == 0.0.0.dev53",
    "PyLinks == 0.0.0.dev37",
    "PkgData == 0.0.0.dev5",
    "PySerials == 0.0.0.dev27",
    "MDit == 0.0.0.dev24",
    "ExceptionMan == 0.0.0.dev24",
    "platformdirs >= 4.3, < 5",
]
