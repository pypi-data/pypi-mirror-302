# setup.py

from setuptools import setup, find_packages

setup(
    name='printd-Tl',
    version='0.1',
    packages=find_packages(),
    description='A package to print experiment codes for Python.',
    author='code-tl'
)
