import subprocess
import sys

def main():
    """Entry point for the Python package wrapper."""
    print("The Python wrapper is disabled. Please use the 'spacejar' command directly.")
    print("If 'spacejar' is not found, ensure it's in your PATH or reinstall the package.")
    sys.exit(1)

if __name__ == "__main__":
    main()